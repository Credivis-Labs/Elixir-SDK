/**
 * Elixir Classic — plain G-account multisig (up to 20 signers, weights,
 * low/med/high thresholds).
 *
 * Serves users the smart account structurally CANNOT: anchors, SDEX market
 * makers, anyone needing classic ops (offers, ChangeTrust, path payments).
 * No contract risk, no audit gate, no relayer, no TTL rent.
 *
 * Recommended as the v1 product — see docs/GAPS.md §F1.
 */

export {};
